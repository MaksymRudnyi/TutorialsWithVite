Styled components vs Emotion:
    Styled:
        Large and active community
        Theming support
        Performance (may be slow)
    Emotion:
        Focus on performance and has optimizations for server-side rendering
        Smaller bundle size
        Smaller community (show NPM trands)


Inline styles:
    No pseudo selectors (:hover, :active)
    No media queries (just with additional CSS)


Tailwind:
    Configuration
    learn all these classes
